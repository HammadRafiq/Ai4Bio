export const styles = {
  customButtonStyle: (padding: any, fontSize: any) => ({
    color: "#fff",
    fontWeight: "500",
    borderRadius: "40px",
    textTransform: "capitalize",
    background: "linear-gradient(90deg, #26ACE2 1.99%, #9E83BD 100%)",
    padding: padding || "8px 30px",
    fontSize: fontSize || "18px",
  }),
};
